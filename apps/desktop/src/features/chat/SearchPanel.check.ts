import assert from 'node:assert/strict';
import { SearchPanel } from './SearchPanel';

assert.equal(typeof SearchPanel, 'function', 'SearchPanel should be a function component');

console.log('SearchPanel.check.ts ok');
